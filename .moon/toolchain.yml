$schema: 'https://moonrepo.dev/schemas/toolchain.json'

# Toolchain management is DISABLED - using Nix instead
# Node.js, Bun, and other tools are managed via Nix for reproducible environments
# See flake.nix for tool versions and configuration

# Empty configuration - moon will use system/Nix-provided tools
